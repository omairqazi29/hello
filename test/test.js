'use strict';
var expect = require('chai').expect;
var index = require('../index.js');
describe('omair function test', () => {
    it('should return Hello World!', () => {
        var result = 'Hello World!';
        expect(result).to.equal('Hello World!');
    });
});
